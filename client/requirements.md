## Packages
framer-motion | Page transitions and scroll-triggered animations
react-hook-form | Form state management
@hookform/resolvers | Zod validation integration for forms

## Notes
- Bilingual app (Arabic/Hebrew) built with RTL as the default direction.
- Uses dynamic font switching based on the HTML `lang` attribute (Cairo for AR, Heebo for HE).
- Primary colors: Dark Navy Blue and Bright Cyan.
- Static image `@assets/pp_1772668661989.png` used for the clinic logo.
